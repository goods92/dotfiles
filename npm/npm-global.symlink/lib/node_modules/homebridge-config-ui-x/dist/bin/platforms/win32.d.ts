import { HomebridgeServiceHelper } from '../hb-service';
export declare class Win32Installer {
    private hbService;
    constructor(hbService: HomebridgeServiceHelper);
    install(): Promise<void>;
    uninstall(): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
    restart(): Promise<void>;
    rebuild(all?: boolean): Promise<void>;
    getId(): Promise<{
        uid: number;
        gid: number;
    }>;
    getPidOfPort(port: number): any;
    updateNodejs(job: {
        target: string;
        rebuild: boolean;
    }): void;
    private checkIsAdmin;
    private downloadNssm;
    private configureFirewall;
}
