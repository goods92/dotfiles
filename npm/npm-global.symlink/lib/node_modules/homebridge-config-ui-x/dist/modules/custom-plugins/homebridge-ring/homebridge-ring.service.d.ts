import { HttpService } from '@nestjs/common';
import { Logger } from '../../../core/logger/logger.service';
import { HomebridgeRingCredentialsDto } from './homebridge-ring.dto';
export declare class HomebridgeRingService {
    private logger;
    private httpService;
    constructor(logger: Logger, httpService: HttpService);
    exchangeCredentials(credentials: HomebridgeRingCredentialsDto): Promise<any>;
}
